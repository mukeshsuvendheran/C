#include<stdio.h>
int main()
{
    float a,b;
    printf("enter the two values");
    scanf("%f%f",&a,&b);
    printf("addition (a+b) = %0.2f",a+b);
    printf("subraction (a-b) = %0.2f",a-b);
    printf("multiplication (a*b) = %0.2f",a*b);
    printf("subracton (a/b) = %0.2f",a/b);
  //  printf("division (a%b) = %f",a%b);
}